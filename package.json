{
  "name": "alhambrasoap",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
